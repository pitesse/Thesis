\documentclass[a4paper,twocolumn]{article}

%------------------------------------------------------------------------------
%   POLIMI STYLE AND PACKAGES
%------------------------------------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\geometry{top=3cm, bottom=2.5cm, left=1.5cm, right=1.5cm}

\usepackage{color}
\usepackage{xcolor}
\definecolor{bluepoli}{cmyk}{0.4,0.1,0,0.4} % The official PoliMi Blue

\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{epstopdf}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{caption}


\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{listings}
\usepackage{cite}

\titleformat{\section}
{\color{bluepoli}\normalfont\Large\bfseries}
{\color{bluepoli}\thesection}{1em}{}

\titleformat{\subsection}
{\color{bluepoli}\normalfont\large\bfseries}
{\color{bluepoli}\thesubsection}{1em}{}

\graphicspath{{./Images/}}

\setlength{\headheight}{85pt}
\addtolength{\topmargin}{-35pt}
\pagestyle{fancy}
\fancyhf{}

\fancyhead[L]{\includegraphics[height=1.8cm]{logo_polimi_scritta2}}
\fancyhead[R]{\bfseries\color{bluepoli} F1 Strategy Operations}
\fancyfoot[C]{\thepage}

\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{bluepoli}\leaders\hrule height \headrulewidth\hfill}}

\lstset{
  basicstyle=\ttfamily\scriptsize,
  breaklines=true,
  breakatwhitespace=true,
  columns=fullflexible,
  keepspaces=true,
  linewidth=\columnwidth,
    frame=single,
    numbers=left,
    numberstyle=\tiny\color{gray},
  xleftmargin=2.2em,
  framexleftmargin=2.2em,
  framesep=2pt,
    keywordstyle=\color{bluepoli},
    commentstyle=\color{green!60!black},
    stringstyle=\color{red},
    showstringspaces=false,
    captionpos=b,
    rulecolor=\color{bluepoli}
}

%------------------------------------------------------------------------------
%   DOCUMENT CONTENT
%------------------------------------------------------------------------------
\begin{document}

\twocolumn[{

      \begin{center}
        {\LARGE \bfseries \color{bluepoli} Real-time F1 Strategy Operations:\\ A Stream Processing Architecture for Pit Wall Simulation}\\[1em]
        {\large Pietro Pizzoccheri}\\[0.5em]
        {\itshape Department of Electronics, Information and Bioengineering}\\
        {\itshape Politecnico di Milano, Milan, Italy}\\
        {\ttfamily pietro.pizzoccheri@mail.polimi.it}\\[1em]
      \end{center}


      \vspace{1em}


      \begin{abstract}
        This report presents a stream processing architecture that simulates
        a Formula~1 pit wall decision support system. The pipeline ingests
        historical race telemetry through a Python producer backed by the
        \texttt{fastf1} library, decouples it via three dedicated Apache Kafka
        topics, and processes it in Apache Flink 1.20 using event-time
        semantics, stateful operators, and Complex Event Processing (CEP).
        The system is structured in three modules: \emph{Context Awareness}
        (track status enrichment, rival identification, DRS train detection),
        \emph{Ground Truth Generation} (pit stop outcome classification via a
        three-stage state machine with dual-rival cluster tracking, offset
        strategy detection, and eight distinct labels normalized as
        track-agnostic percentages), and \emph{Real-Time Alerts} (lift \&
        coast detection, tire degradation monitoring, physical race
        drop zone analysis, and continuous fuzzy-logic pit strategy scoring
        with broadcast-state Safety Car urgency).
        Special attention is given to production-grade memory management
        through State TTL, low-latency event propagation through broadcast
        state patterns, and to the elimination of score clumping through
        continuous mathematical scoring curves (power, linear interpolation,
        quadratic). The architecture is validated on multiple historical
        races, demonstrating reproducible, deterministic processing
        independent of wall-clock execution speed.
      \end{abstract}

      \vspace{1em}
    }]

\section{Introduction}

Pit wall strategy in Formula~1 involves continuous, multi-dimensional
decision-making under tight latency constraints. Engineers must synthesize
car telemetry arriving at several hertz, per-lap timing summaries, tire
degradation trends, and evolving track conditions, all while evaluating
whether a pit stop at the current moment would gain or lose track position.
Historically this expertise resides in the intuition of experienced
strategists, making it difficult to reproduce, benchmark, or scale.

The objective of this project is to construct a streaming data pipeline
that replays historical F1 race data with original inter-sample timing,
processes it through stateful operators that encode the business logic of a
pit wall, and produces structured output suitable for building a strategic
ground truth dataset. Such a dataset forms the foundation for future
machine learning models that can predict optimal pit windows, classify
undercut opportunities, and forecast tire degradation cliffs.

\subsection{Scope and Module Division}

The processing logic is divided into three modules reflecting distinct
strategic concerns:

\begin{itemize}
  \item \textbf{Module~A: Context Awareness.} Maintains a global view
    of the race state: current track status (green, Virtual Safety Car,
    Safety Car), identification of the car ahead and behind each driver
    with gap deltas, and detection of DRS trains where multiple cars are
    trapped within one second of each other.
  \item \textbf{Module~B: Ground Truth Generation.} Classifies each
    observed pit stop into one of eight labels
    (\textsc{success\_undercut}, \textsc{success\_overcut},
    \textsc{success\_defend}, \textsc{success\_free\_stop},
    \textsc{offset\_advantage}, \textsc{offset\_disadvantage},
    \textsc{failure\_pace\_deficit}, \textsc{failure\_traffic})
    using a three-stage state machine that tracks rival clusters (car
    ahead and behind), detects offset strategies (1-stop vs.\ 2-stop via
    timeout), and normalizes gap deltas as a percentage of the driver's
    stint-best lap time for track-agnostic cross-circuit comparison.
  \item \textbf{Module~C: Real-Time Alerts.} Detects fuel-saving
    lift-and-coast maneuvers via CEP pattern matching, monitors tire
    performance degradation relative to a rolling stint baseline,
    computes the physical race emergence position after a hypothetical
    pit stop by walking the full position ladder, and produces continuous
    fuzzy-logic pit desirability scores (0--100) using five mathematical
    curves with broadcast-state Safety Car urgency fast-path.
\end{itemize}

The remainder of this report is organized as follows: Section~II
describes the architecture and data flow. Section~III discusses
latency and time management. Section~IV details the correctness of
each processing rule. Section~V presents evaluation results on
historical races. Section~VI concludes with future directions.

\section{Architecture and Data Flow}

The system follows a producer--broker--consumer architecture where each
component is independently deployable. The Python producer and the Flink
processor communicate exclusively through Kafka, enabling independent
scaling and fault isolation.

\subsection{Producer: Historical Replay with Original Timing}

The producer is a Python script that loads a historical F1 session via the
\texttt{fastf1} library, which provides access to official FIA timing data
including per-sample car telemetry, per-lap summaries, and track status
changes. The replay mechanism merges all drivers' telemetry into a single
chronologically sorted dataframe and emits each row to Kafka with a
\texttt{time.sleep()} delay proportional to the original $\Delta t$
between consecutive samples, scaled by a configurable speed multiplier.
This preserves the relative timing characteristics of the original race:
high-frequency telemetry bursts during straights, gaps during pit stops,
and sparse track status transitions.

The producer is parameterized via command-line arguments (\texttt{--year},
\texttt{--race}, \texttt{--session}, \texttt{--speed},
\texttt{--start-lap}), allowing any historical session from 2018 onward to
be replayed without code changes.

\subsection{Kafka: Three-Topic Decoupling}

A central architectural decision is the separation of race data into three
Kafka topics with fundamentally different arrival rates:

\begin{enumerate}
  \item \textbf{\texttt{f1-telemetry}} ($\sim$4\,Hz per driver):
    per-sample car data including speed, throttle, brake, gear, RPM, and
    DRS state.
  \item \textbf{\texttt{f1-laps}} ($\sim$1 event per driver every
    $\sim$80\,s): per-lap summaries with lap time, sector times, position,
    compound, tire life, stint number, and pit in/out timestamps.
  \item \textbf{\texttt{f1-track-status}} ($\sim$1--5 per race): global
    track status transitions (green, yellow, VSC, Safety Car, red flag).
\end{enumerate}

This separation is not merely organizational---it is required for correct
watermark progression in Flink. If all three event types were multiplexed
onto a single topic, the sparse track status events (minutes apart) would
prevent Flink from advancing the global watermark past the last track
status timestamp, effectively stalling all downstream operators that depend
on event-time progress. With separate sources, each stream advances its
watermark independently, and the idleness mechanism (Section~III)
prevents the sparsest stream from blocking the pipeline.

\subsection{Flink: Stateful Stream Processing}

The Flink job (\texttt{F1StreamingJob}) consumes all three Kafka sources
at parallelism~1, deserializes JSON payloads via Jackson into typed POJOs
(\texttt{TelemetryEvent}, \texttt{LapEvent}, \texttt{TrackStatusEvent}),
assigns event-time watermarks, and routes events through the three
processing modules. The job accepts runtime parameters via Flink's
\texttt{ParameterTool} (e.g., \texttt{--pit-loss 22.0}) to adapt
thresholds to different circuits.

Deserialization uses a generic \texttt{RichFlatMapFunction<String,\,T>}
that silently drops malformed records with a warning log rather than
failing the pipeline. Explicit \texttt{.returns()} type hints are required
after each \texttt{flatMap} and lambda \texttt{map} to work around Java
type erasure in Flink's generic type inference.

\section{Latency and Time Management}

Correct time semantics are the foundation of reproducible stream
processing. This section details the choices made to ensure that the
pipeline produces identical results regardless of the wall-clock speed at
which the producer replays the race.

\subsection{Event Time over Processing Time}

All operators in the pipeline operate on \emph{event time} extracted from
the ISO-8601 \texttt{Date} field embedded in each JSON payload by the
Python producer. This field carries the original race timestamp (e.g.,
\texttt{2023-09-03T13:05:12.003Z}), which is converted to epoch
milliseconds at deserialization time via \texttt{Instant.parse()} or
\texttt{LocalDateTime.parse()} with a UTC assumption.

Using event time rather than processing time ensures that:
\begin{itemize}
  \item Running the producer at $50\times$ speed produces the same output
    as $1\times$ speed---only wall-clock duration changes.
  \item Network delays, garbage-collection pauses, and container
    scheduling jitter do not affect window boundaries or timer firings.
  \item The same Kafka recording can be replayed months later with
    identical results, enabling reproducible ground truth datasets.
\end{itemize}

\subsection{Watermark Strategy}

Watermarks are assigned per-source using
\texttt{BoundedOutOfOrderness(Duration.ofSeconds(5))}. The 5-second bound
accommodates two sources of disorder:

\begin{enumerate}
  \item \textbf{Producer jitter:} the Python producer iterates rows
    sequentially with per-row sleeps, so events for different drivers at
    nominally the same race timestamp may arrive several hundred
    milliseconds apart.
  \item \textbf{Kafka partition ordering:} within a single partition,
    ordering is guaranteed, but the consumer may observe minor
    out-of-orderness when reading from the partition buffer during
    high-throughput bursts.
\end{enumerate}

A bound tighter than 5 seconds risks dropping late telemetry events; a
bound significantly larger would delay watermark advancement and increase
output latency. The 5-second value was empirically validated across
multiple races at replay speeds of $1\times$ to $100\times$.

\subsection{Idle Source Handling for Sparse Streams}

The track status stream presents a unique challenge. During a clean race,
only three events may arrive over 90~minutes: an initial ``AllClear,'' a
mid-race yellow flag, and a return to green. Between these events, the
source produces no records, and without intervention, its watermark
remains frozen at the timestamp of the last track status event.

In Flink, the global watermark across unionized sources advances at the
pace of the \emph{slowest} source. A stalled track status watermark would
therefore prevent all downstream operators---session windows, CEP
patterns, event-time timers---from making progress.

The solution is the \texttt{withIdleness(Duration.ofSeconds(30))}
configuration on the track status watermark strategy. After 30 seconds
without a new event, Flink marks the source as \emph{idle} and excludes
it from the global watermark computation. The remaining two active
sources (telemetry and laps) then drive watermark advancement unimpeded.
When a new track status event eventually arrives, the source automatically
reactivates and rejoins the watermark union.

The 30-second threshold is chosen to be comfortably longer than the
maximum expected inter-event gap within any single source during normal
operation ($\sim$80\,s for laps, but those advance continuously), yet
short enough that the pipeline does not stall during track status silence.

\section{Correctness of the Rules}

Each processing rule is implemented with a specific Flink operator pattern
chosen to match the semantics of the underlying business logic. This
section details each rule and justifies the operator selection.

\subsection{Module A: Context Awareness}

\subsubsection{Track Status Enrichment (Broadcast State)}

Track status is a global, unkeyed dimension: all drivers on track share
the same flag status at any given moment. Flink's \emph{broadcast state}
pattern is the canonical mechanism for distributing such a slowly-changing
dimension across a keyed stream. The \texttt{TrackStatusEnricher} extends
\texttt{KeyedBroadcastProcessFunction}: the broadcast side writes the
latest status code to a \texttt{MapStateDescriptor} on each incoming
\texttt{TrackStatusEvent}, while the keyed side reads it for each
\texttt{TelemetryEvent} and attaches the current status before forwarding.

This approach avoids the semantically incorrect alternative of keying the
track status by driver (which would require duplicating every status event
per driver) and the operationally fragile alternative of using a static
variable (which would not survive state recovery after a checkpoint
restore).

\subsubsection{Rival Identification (Session Windows)}

Identifying the car ahead and behind each driver requires collecting all
drivers' lap events for the same lap and sorting by position. This is
implemented as a \texttt{ProcessWindowFunction} over an
\texttt{EventTimeSessionWindow} with a 30-second gap, keyed by lap
number.

Session windows were chosen over tumbling or sliding windows because the
absolute timestamps of lap completions vary from race to race and lap to
lap. A session window with a 30-second gap naturally groups all drivers'
events for the same lap (which arrive within seconds of each other due to
the producer's chronological interleaving) without requiring explicit
knowledge of lap boundaries. The 30-second gap is generous enough to
tolerate producer jitter and replay speed variation, yet short enough that
events from consecutive laps (separated by $\sim$80\,s at $1\times$
speed) are never incorrectly merged.

\subsubsection{DRS Train Detection}

A DRS train is a contiguous sequence of cars where each consecutive pair
is separated by less than one second, making overtaking with DRS
ineffective due to the preceding car also having DRS. The
\texttt{DrsTrainDetector} operates as a second-stage
\texttt{ProcessWindowFunction} on the \texttt{RivalInfo} output, windowed
again by lap number with a 10-second session gap. Within each window, it
sorts rivals by position and scans for contiguous groups where
\texttt{gapAhead < 1.0\,s}, emitting an alert for each group of two or
more cars.

\subsection{Module B: Ground Truth}

\subsubsection{Pit Stop Evaluator (Three-Stage State Machine)}

The \texttt{PitStopEvaluator} implements a three-stage state machine as a
\texttt{KeyedProcessFunction<String, LapEvent, PitStopEvaluation>}, keyed
by a constant \texttt{"RACE"} for global field visibility---the same
leader-driven pattern used by the Drop Zone Evaluator. The evaluator
stores all lap events in a flat \texttt{MapState} (key format
\texttt{"lapNumber:driver"}) and maintains a \texttt{MapState} of
pending pit cycles awaiting resolution.

The core insight is that raw position comparison always labels pit stops
as failures because pitting costs $\sim$22--25\,s and drops a driver
multiple positions. Instead, the evaluator measures the \emph{directed
time gap} between the pitting driver and their rivals before and after
the pit stop settles, normalized as a percentage of the driver's
stint-best lap time for track-agnostic comparison.

\paragraph{Rival cluster tracking.}
At pit entry, the evaluator captures both the car ahead (undercut
target, position~$-1$) and the car behind (defense target,
position~$+1$). Gap computation walks the position ladder at a given
lap, summing \texttt{gapToCarAhead} from each intermediate position.
A lap-1 fallback mechanism handles \emph{sequential arrival}: when a
driver pits on lap~$N$, the car behind may not yet have been processed
for lap~$N$ (it crosses the line later), so the evaluator falls back to
lap~$N-1$ state and computes gaps on the lap where the rival was
actually found.

\paragraph{State machine stages.}
The evaluation protocol proceeds through three explicit states:
\begin{enumerate}
  \item \textbf{\textsc{pending\_settle}:} After pit detection
    (\texttt{pitInTime $\neq$ null}), the evaluator waits for 3~post-pit
    laps to elapse, allowing the out-lap (unrepresentative due to cold
    tires) and initial recovery to complete.
  \item \textbf{\textsc{pending\_rival}:} Monitors whether the net rival
    has also pitted. If the rival's stint changes (indicating a pit stop),
    the cycle resolves immediately. An overcut lookback window checks
    whether the rival pitted \emph{before} the driver by scanning a range
    of recent laps.
  \item \textbf{Offset timeout resolution:} If the rival has not pitted
    within 40\% of remaining race laps, the cycle is classified as an
    offset strategy (e.g., 1-stop vs.\ 2-stop) rather than waiting
    indefinitely. A 15-minute event-time safety timer provides a hard
    upper bound for edge cases (retirements, race-ending scenarios).
\end{enumerate}

\paragraph{Track-agnostic percentage scoring.}
Rather than comparing absolute gap values in seconds, the evaluator
normalizes gap deltas as a percentage of the driver's stint-best lap
time:
\[
  \mathit{gapDeltaPct} = \frac{\mathit{postGap} - \mathit{preGap}}{\mathit{baselineLap}} \times 100
\]
This enables meaningful cross-track comparison: a 2\,s gap change at
Monaco ($\sim$78\,s lap $\approx$ 2.6\%) carries more strategic
significance than the same 2\,s at Spa ($\sim$105\,s $\approx$ 1.9\%).

\paragraph{Eight-label classification.}
Based on the normalized gap delta, pit order, track status, and
emergence traffic, the evaluator assigns one of eight labels:
\begin{itemize}
  \item \textsc{success\_undercut}: driver pitted first, gained
    $> 0.5\%$ relative to rival.
  \item \textsc{success\_overcut}: rival pitted first, driver still
    gained $> 0.5\%$ by staying out.
  \item \textsc{success\_defend}: gap change within $\pm 0.5\%$
    tolerance band.
  \item \textsc{success\_free\_stop}: pitted under SC/VSC with
    $< 1.0\%$ loss (minimal time cost under caution).
  \item \textsc{offset\_advantage}/\textsc{offset\_disadvantage}:
    different strategy confirmed via timeout, classified by gap
    trajectory.
  \item \textsc{failure\_pace\_deficit}: lost $> 0.5\%$ to rival.
  \item \textsc{failure\_traffic}: emerged into dirty air despite
    theoretical gap win (car ahead with tyre life $\geq 25$ laps).
\end{itemize}

\paragraph{State TTL for production memory management.}
All three \texttt{MapState} descriptors (\texttt{lapEvents},
\texttt{pendingCycles}, \texttt{stintBests}) are configured with Flink's
State TTL mechanism set to 2~hours, using \texttt{OnCreateAndWrite}
update type and \texttt{NeverReturnExpired} visibility. This prevents
unbounded state growth over continuous streaming sessions (e.g.,
season-level bulk processing) while ensuring that state remains
accessible for the full duration of any single race ($\sim$90~min).

\subsection{Module C: Real-Time Alerts}

\subsubsection{Lift and Coast Detection (CEP)}

Lift-and-coast is a fuel-saving technique where the driver releases the
throttle before the normal braking point and coasts to the corner. The
detection pattern is implemented using Flink's CEP library as a
three-step sequence within a 4-second window:

\begin{enumerate}
  \item \texttt{full-throttle}: \texttt{Throttle == 100}
  \item \texttt{lift}: \texttt{Throttle == 0}
  \item \texttt{coast-brake}: \texttt{Brake > 0}
\end{enumerate}

The pattern uses \texttt{followedBy()} (relaxed contiguity) rather than
\texttt{next()} (strict contiguity) between each step. This is essential
because real telemetry contains intermediate throttle values (e.g., 80,
60, 40) and gear-change events between the full-throttle phase and the
complete lift. Strict contiguity would require the very next event to
match, which almost never happens with 4\,Hz telemetry where throttle
release is gradual over several samples.

However, \texttt{followedBy()} with relaxed contiguity creates a
combinatorial problem: if $N$ consecutive events have
\texttt{Throttle == 100}, each independently matches as a
\texttt{full-throttle} start, and all $N$ matches converge on the same
\texttt{lift} and \texttt{coast-brake} events, producing $N$ duplicate
alerts for a single braking zone.

Two complementary mechanisms address this:
\begin{itemize}
  \item \textbf{Skip strategy:}
    \texttt{AfterMatchSkipStrategy.skipPastLastEvent()} causes the CEP
    engine to resume scanning after the \texttt{coast-brake} event of each
    completed match, preventing earlier throttle samples from re-matching.
    This reduced duplicates by $\sim$75\% in empirical testing.
  \item \textbf{Post-CEP deduplication:} A downstream
    \texttt{KeyedProcessFunction} (\texttt{LiftCoastDedup}) maintains per-driver
    \texttt{ValueState<String>} storing the brake timestamp of the last
    emitted alert. Any new alert with an identical brake timestamp is
    suppressed. This eliminates the remaining duplicates caused by
    multiple NFA instances completing simultaneously on the same brake
    event, achieving zero actual duplicates while preserving all
    legitimate alerts.
\end{itemize}

\subsubsection{Tire Degradation Monitoring}

The \texttt{TireDropDetector} is a \texttt{KeyedProcessFunction} that
maintains per-driver state: the current stint number, the best clean lap
time in the stint, and a rolling buffer of the last three lap times. On
each new clean lap (excluding pit in-laps and out-laps), the rolling
3-lap average is compared to the stint best. When the delta exceeds
1.5~seconds, a \texttt{TireDropAlert} is emitted carrying the driver,
lap number, compound, tire age, average time, best time, and delta.

State is automatically reset when a stint change is detected (the
\texttt{stint} field changes between consecutive \texttt{LapEvent}s),
establishing a fresh baseline for the new tire set. This ensures that
degradation is always measured relative to the current compound's peak
performance.

\subsubsection{Drop Zone Analysis (Leader-Driven State Machine)}

The \texttt{DropZoneEvaluator} computes the physical race emergence
position for each driver: the exact track position they would occupy
after a pit stop, given the full position ladder and a track-specific pit
loss value. This replaces the original threshold-based
\texttt{PitWindowDetector} with a fundamentally different approach that
models both the \emph{net race} (classification rivals for championship
points) and the \emph{physical race} (on-track traffic after pitting).

\paragraph{The idle partition problem.}
During development, the evaluator was initially keyed by
\texttt{lapNumber} with an event-time timer that would fire after all
events for a given lap had arrived. However, this approach failed
silently: because all events for lap~$N$ arrive within a brief window
and then the keyed partition for lap~$N$ receives no further data, the
local watermark stalls indefinitely. The event-time timer, which requires
the watermark to advance past its target timestamp, never fires.
Processing-time timers were considered as an alternative but rejected
because they break determinism under the 100$\times$ fast-forward replay:
at high speed multipliers, processing-time timers fire at wall-clock
intervals that no longer correspond to race time.

\paragraph{Leader-driven trigger.}
The solution exploits a rule of race physics: when the race leader
(position~1) completes lap~$N$, every other car on track has necessarily
completed lap~$N-1$. All lap events therefore flow into a single keyed
partition (keyed by the constant \texttt{"RACE"}). When the leader's
lap~$N$ event arrives, evaluation of the lap~$N-1$ position ladder is
triggered immediately, without any timers, watermark dependencies, or
idle-source considerations.

\paragraph{State design.}
The operator uses a flat \texttt{MapState<String,\,LapEvent>} with
composite keys of the form \texttt{"lapNumber:driver"} (e.g.,
\texttt{"15:VER"}). An earlier iteration used
\texttt{MapState<Integer,\,List<LapEvent>{>}} with Flink's
\texttt{Types.LIST(Types.POJO(...))} serializer, which silently failed:
the lists appeared empty when accessed despite successful \texttt{put()}
calls. The flat-map approach avoids nested generic serialization entirely
and has proven robust across hundreds of laps per race.

\paragraph{Algorithm.}
For each driver at position~$P$ with pit loss~$L$, the evaluator walks
the position ladder from $P+1$ downward, accumulating the
\texttt{gapToCarAhead} value at each successive position. When the
cumulative gap exceeds~$L$, the driver emerges between the previous and
current car, and the remaining gap to the physical car ahead is computed
as $L$ minus the cumulative gap up to the previous car. If the entire
ladder is exhausted without exceeding~$L$, the driver drops to the back
of the field. Only drivers with tyre life $\geq 8$~laps are evaluated,
filtering out opening-lap noise and immediate post-pit evaluations.
Pit loss values are track-specific constants embedded by the Python
producer (\texttt{prepare\_race.py}): green-flag, VSC, and Safety Car
variants for each circuit. Evaluation is suppressed under yellow or red
flag conditions where the pit lane may be closed.

The output for each driver includes the net rival (car immediately ahead
in classification), the physical car ahead after pitting, the compound
and tyre age of that car (critical for assessing whether the traffic is
slow enough to pass), and the total positions lost.

\subsubsection{Pit Strategy Evaluator (Continuous Fuzzy Logic with
Broadcast Urgency)}

The \texttt{PitStrategyEvaluator} extends
\texttt{KeyedBroadcastProcessFunction} to produce a continuous
pit desirability score ($0.0$--$100.0$) for each driver on every lap,
eliminating the discrete threshold clumping that characterized the
original implementation. The operator receives two inputs: the keyed
lap stream via \texttt{processElement()} and the broadcast track status
via \texttt{processBroadcastElement()}.

\paragraph{Why broadcast state for urgency.}
When a Safety Car or Virtual Safety Car deploys, the strategic value of
pitting changes instantly: pit loss drops from $\sim$22\,s to
$\sim$12\,s, making a ``free stop'' available for all drivers. Waiting
for the next lap completion event ($\sim$80\,s latency) is
unacceptable. Instead, the operator receives the track status transition
via broadcast state and immediately re-evaluates all drivers using their
most recently cached lap data, reducing latency to $\sim$0--5\,s. This
reuses the same broadcast state pattern as
\texttt{TrackStatusEnricher} but with a separate state descriptor to
avoid cross-contamination.

\paragraph{Five continuous scoring curves.}
Each dimension maps to its output range through a continuous
mathematical function:

\begin{enumerate}
  \item \textbf{Pace} ($0$--$30$): $30 \cdot \min\!\big(1,\, (r / 0.03)^{1.5}\big)$,
    where $r = (\mathit{lapTime} - \mathit{stintBest}) / \mathit{stintBest}$.
    The power-1.5 curve penalizes gently at low degradation and
    aggressively at high: 1\% $\to 8.1$, 2\% $\to 21.8$,
    $\geq$3\% $\to 30$.
  \item \textbf{Traffic} ($-30$ to $+30$): linear interpolation on the
    emergence gap computed via the same position-ladder walk used by the
    Drop Zone Evaluator. $\geq$3\,s yields $+30$ (clean air), 1--3\,s
    interpolates linearly to~0, 0--1\,s (DRS danger zone) interpolates
    to $-30$. A $+5$ bonus is added when the car ahead has
    \texttt{tyreLife} $\geq 25$ (easy pass on degraded tires).
  \item \textbf{Urgency} ($0$--$30$): $30 \cdot \min\!\big(1,\,
    \big((\tau - 0.5) / 0.5\big)^{2}\big)$, where
    $\tau = \mathit{tyreLife} / \mathit{maxStint}$. Quadratic ramp:
    zero until 50\% of the expected stint, then accelerates. 70\%
    $\to 4.8$, 90\% $\to 19.2$, $\geq$100\% $\to 30$.
  \item \textbf{Strategy penalty} ($-15$ to $0$): continuous penalty
    when the next tire compound cannot cover the remaining race
    distance. Scales linearly with the deficit, amplified by a factor
    of~3 and capped at $-15$.
  \item \textbf{Track status} ($0$ or $60$): the only crisp metric.
    SC/VSC is a binary event with massive strategic impact---there is
    no ``partially under safety car.''
\end{enumerate}

\paragraph{Multi-label output.}
The total score is clamped to $[0, 100]$ and classified into discrete
labels: \textsc{monitor} (40--59), \textsc{good\_pit} (60--79),
\textsc{pit\_now} ($\geq$80), and \textsc{lost\_chance} (emitted once
per stint when the peak score was $\geq$70 but has dropped below~40
while degradation continues to worsen).

\paragraph{Emit-gate logic.}
To prevent notification spam, the operator tracks the last emitted
score and track status per driver within each stint. After the first
alert, re-emission requires either a $\geq$10-point score escalation
or a track status change (new strategic opportunity).

\paragraph{State TTL.}
All nine \texttt{MapState} descriptors are configured with 2-hour
State TTL (\texttt{OnCreateAndWrite}, \texttt{NeverReturnExpired}),
ensuring production-grade memory management during continuous
season-level data generation.

\section{Evaluation and Validation}

The system was validated on multiple historical races with different
characteristics to exercise all processing rules under varied conditions.

\subsection{2023 Italian Grand Prix (Monza)}

The primary development and validation target was the 2023 Italian Grand
Prix, selected for its well-documented pit stop outcomes and DRS-dependent
racing at a power-sensitive circuit. The producer dynamically extracts all
drivers present in the session data (typically 20 on a full grid), scaling
linearly with grid size. With track-specific pit loss values
(green=25.0s, VSC=15.0s, SC=12.0s) and the full 20-driver grid, the
system processed 316\,517 telemetry events, 929 lap events, and 3 track
status transitions and produced:
\begin{itemize}
  \item \textbf{Lift \& Coast:} 76 unique alerts across the race
    distance, with VER producing the most detections (tire management
    in the second half of the race), followed by HUL, RUS, SAI, PER,
    and LAW. The 1-second minimum coast duration filter, calibrated
    from fastf1 telemetry analysis showing genuine lift \& coast at
    Monza lasts 0.8--1.5s, effectively separates fuel-saving from
    normal corner braking.
  \item \textbf{Drop Zone:} 664 position-ladder evaluations across all
    laps. Early-lap alerts correctly showed most drivers dropping to
    P20 (behind the sole hard-tire starter MAG), as the field had not
    yet developed sufficient time gaps. By mid-race, gaps widened
    and emergence positions improved for front-runners.
  \item \textbf{Tire Drop:} alerts correctly identifying degradation
    across the grid in the closing laps of each stint, with compound-dependent
    thresholds (SOFT=1.0s, MEDIUM=1.5s, HARD=2.0s) triggering at
    appropriate severity levels.
  \item \textbf{DRS Train:} extensive train detection across the field,
    capturing the densely packed midfield characteristic of modern F1.
  \item \textbf{Pit Evaluations:} $\sim$20 classifications (one per
    driver per pit stop), all consistent with historical race records.
\end{itemize}

All 929 lap events, rival identification records, and track status
transitions were processed with zero deserialization errors.

\subsection{2023 Australian Grand Prix (Melbourne)}

The Australian Grand Prix was used to validate the drop zone evaluator
and tire drop detector under Safety Car and red flag conditions. The
race's multiple Safety Car periods and a red-flag stoppage exercised
the broadcast state transitions and the evaluator's yellow/red
suppression logic: under yellow or red flags, the
\texttt{DropZoneEvaluator} correctly suppressed all alerts (pit lane
may be closed), while under VSC the reduced pit loss value (13.0s
for Melbourne) was correctly selected, producing significantly fewer
positions-lost predictions compared to green-flag conditions.

\subsection{2024 Italian Grand Prix (Monza)}

A second Monza race (2024 season) was used to validate tire degradation
alerts across different tire compounds and to verify that the drop zone
evaluator correctly adapts to different grid configurations and race
dynamics compared to the 2023 edition.

\subsection{Unit Testing with Flink Test Harness}

Beyond end-to-end validation, the stateful operators were tested in
isolation using Flink's \texttt{KeyedOneInputStreamOperatorTestHarness}.
This approach wraps a \texttt{KeyedProcessFunction} in a lightweight
harness that supports programmatic element injection, timestamp
assignment, and output extraction---without requiring a running Flink
cluster or Kafka broker.

Two test suites were implemented:
\begin{itemize}
  \item \textbf{TelemetryEventTest} (7 tests): validates the
    \texttt{parseEventTime()} timestamp parser across ISO-8601 variants
    (with and without timezone suffix), whole-second precision, and
    graceful handling of null, empty, and malformed strings.
  \item \textbf{PitStopEvaluatorTest} (8 tests): exercises the
    three-stage state machine by injecting sequences of
    \texttt{LapEvent} records with controlled positions,
    \texttt{gapToCarAhead} values, pit-in/pit-out timestamps, and stint
    numbers. The suite covers all eight classification labels:
    \textsc{success\_undercut} (driver pits first, gains position),
    \textsc{success\_overcut} (rival pits first, driver gains by staying
    out), \textsc{success\_defend} (gap maintained within tolerance),
    \textsc{success\_free\_stop} (pit under Safety Car with minimal
    loss), \textsc{offset\_advantage} (rival never pits, different
    strategy confirmed via timeout), \textsc{failure\_pace\_deficit}
    (gap worsens after pit), no-output when no pit entry occurs, and
    rival cluster capture (both car ahead and behind correctly
    identified with directed gaps, including lap-1 fallback for
    sequential arrival).
\end{itemize}

All 15 tests pass deterministically, confirming that the stateful logic
behaves correctly in isolation before integration into the full pipeline.

\section{Conclusion}

This report presented a stream processing architecture for simulating
Formula~1 pit wall decision support using Apache Flink. The system
ingests historical race data through a parameterized Python producer,
decouples it via three Kafka topics aligned with their natural arrival
rates, and processes it through stateful operators that encode context
awareness, ground truth generation, and real-time alerting logic.

The key technical contributions are:
\begin{enumerate}
  \item A watermark strategy that combines bounded out-of-orderness with
    idle source detection to handle streams with radically different event
    frequencies without pipeline stalls.
  \item A broadcast state pattern for distributing global track status to
    keyed operators, enabling dynamic threshold adaptation and flag-based
    suppression across all alert types.
  \item A CEP-based lift-and-coast detector that uses relaxed contiguity
    with a dual deduplication mechanism (skip strategy + stateful post-filter)
    to achieve zero false duplicates while tolerating noisy telemetry.
  \item A leader-driven drop zone evaluator that bypasses Flink's idle
    keyed partition limitation by exploiting race physics: the leader
    crossing the line guarantees lap completeness for the entire field,
    eliminating all timer and watermark dependencies.
  \item A three-stage pit stop evaluation state machine that tracks
    dual-rival clusters, detects offset strategies via configurable
    timeout, computes track-agnostic gap percentages, and produces
    eight distinct labels covering undercuts, overcuts, free stops,
    and traffic penalties---all suitable for downstream ML training.
  \item A continuous fuzzy-logic pit strategy evaluator using five
    mathematical scoring curves (power, linear interpolation, quadratic,
    linear deficit, binary) with a broadcast-state Safety Car urgency
    fast-path that reduces evaluation latency from $\sim$80\,s (next
    lap completion) to $\sim$0--5\,s (immediate broadcast trigger).
  \item Production-grade memory management via Flink's State TTL
    mechanism on all \texttt{MapState} descriptors (2-hour expiry,
    \texttt{OnCreateAndWrite} update), preventing unbounded state
    growth during continuous season-level data generation.
  \item A CSV data lake via Flink's \texttt{FileSink.forRowFormat} API
    with checkpointing, persisting seven output streams (pit evaluations,
    tire drops, lift \& coast, drop zones, pit suggestions, ML features,
    debug alerts)
    with a custom \texttt{CsvHeaderMapper} that serializes POJO fields
    and ML context features captured at the moment of the strategic
    decision inside the operator's state.
  \item A live Streamlit dashboard consuming three Kafka topics
    (\texttt{f1-laps}, \texttt{f1-track-status}, \texttt{f1-alerts})
    and rendering a two-column pit wall interface with dedicated
    containers for pit strategy (color-coded by severity), pit stop
    evaluations, tire degradation, lift \& coast, alongside a
    leaderboard and gap-to-leader evolution charts.
\end{enumerate}

The architecture is fully parameterized: the Python producer dynamically
extracts the full grid (typically 20 drivers) from any historical race
from 2018 onward, pit loss values are embedded per-track by the producer
ETL stage, and an automation script (\texttt{run\_simulation.sh})
orchestrates the full pipeline from teardown to data collection in a
single command. A season simulator (\texttt{simulate\_season.sh}) replays
all races in a given calendar year sequentially to bulk-generate training
data. All alert and feature streams are persisted to disk as CSV files
via Flink's \texttt{FileSink} with checkpointing enabled, rolling
policies, and Docker volume mapping enabling direct host-side consumption.

\end{document}
