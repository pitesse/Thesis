# unified python dependencies for the f1 pit wall simulation.
# covers producer (fastf1 + kafka), dashboard (streamlit), and ml pipeline (sklearn).
#
# kafka-python-ng is the actively maintained fork of kafka-python.
# both expose the same `kafka` module, so only one can be installed.

# kafka client
kafka-python-ng

# producer: fastf1 api access, data preparation
fastf1
pandas
pyarrow

# dashboard: streamlit web ui
streamlit==1.45.1

# ml pipeline: training and evaluation
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.13
ipykernel>=6.25
joblib>=1.3
